export interface CreateEventTypeDTO {
    code: string;
    name: string;
  
    iconUrl?: string;
    note?: string;

}

export interface UpdateEventTypeDTO {
    code: string;
    name: string;
  
    iconUrl?: string;
    note?: string;

}