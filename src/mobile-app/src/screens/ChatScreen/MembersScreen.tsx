import React from 'react'
import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const MembersScreen = () => {
    return (
        <SafeAreaView>
            <Text style={{color:'red'}}>MembersScreen</Text>
        </SafeAreaView>
    )
}

export default MembersScreen